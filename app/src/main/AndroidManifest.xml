<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >

    <uses-feature
        android:name="vn.usth.team7camera"
        android:required="true" />

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Camera"
        tools:targetApi="31" >

        <activity
            android:name=".MainActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- LoginActivity -->
        <activity
            android:name=".LoginActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity">
        </activity>

        <!-- CameraActivity -->
        <activity
            android:name=".CameraActivity"
            android:exported="true"
            android:parentActivityName=".MainActivity">
        </activity>

        <!-- Add the following for each activity -->
        <!-- VideoQualityActivity -->
        <activity
            android:name=".VideoQualityActivity"
            android:exported="true"
            android:parentActivityName=".MainActivity">
        </activity>

        <!-- MotionDetectionActivity -->
        <activity
            android:name=".MotionDetectionActivity"
            android:exported="true"
            android:parentActivityName=".MainActivity">
        </activity>

        <!-- AudioActivity -->
        <activity
            android:name=".AudioActivity"
            android:exported="true"
            android:parentActivityName=".MainActivity">
        </activity>

        <!-- NightVisionActivity -->
        <activity
            android:name=".NightVisionActivity"
            android:exported="true"
            android:parentActivityName=".MainActivity">
        </activity>

        <!-- LanguageActivity -->
        <activity
            android:name=".LanguageActivity"
            android:exported="true"
            android:parentActivityName=".MainActivity">
        </activity>

        <!-- AboutActivity -->
        <activity
            android:name=".AboutActivity"
            android:exported="true"
            android:parentActivityName=".MainActivity">
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="vn.usth.team7camera.provider"
            android:exported="false"
            android:grantUriPermissions="true" >
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>